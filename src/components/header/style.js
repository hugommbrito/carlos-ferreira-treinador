import styled from "styled-components";

export const StyledHeader = styled.header`
    width: 100vw;
    padding: 0.5rem 0;

    background-color: #000000;
    box-shadow: 0px 14px 8px -8px var(--color-primary);

    position: fixed;
    top: 0;
    z-index: 1;

    img{
        height: 4rem;
    }


`